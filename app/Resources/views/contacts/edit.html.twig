{% extends 'base.html.twig' %}

{% block body %}
    <h1 style="margin-left: 80px;">Modifier un contact</h1>
    <p style="margin-left: 100px;">
        <a href="{{ path('contacts_index') }}">
            <button type="button" class="btn btn-default">Retour à la liste</button>
        </a>
        {{ form_start(delete_form) }}
            <input type="submit" value="Delete" style="margin-left:60%;" class="btn btn-danger">
        {{ form_end(delete_form) }}
    </p>

    <div class="table-striped container" style="width: 80%;">
        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" class="btn btn-default"/>
        {{ form_end(edit_form) }}
    </div>





{% endblock %}
